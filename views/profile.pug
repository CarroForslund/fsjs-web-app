extends layout

block content
  .jumbotron.jumbotron-fluid.justify-content-center
    .container.text-center
      h1 Profile
  if(user)
    h1
      img(src=user.photo alt='avatar')
      | #{user.name}
